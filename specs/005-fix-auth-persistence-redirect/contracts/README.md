# Contracts: Fix Auth Persistence Redirect

This feature does not introduce any external interfaces (APIs, CLI commands, library surfaces). All changes are internal UI logic modifying existing React hooks and components. No contracts needed.

# Contracts: TV Menu Keyboard Navigation

This feature does not introduce any external interfaces (APIs, CLI commands, library surfaces). All changes are internal UI logic modifying existing React components and hooks. No contracts needed.
